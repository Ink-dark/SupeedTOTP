<Window x:Class="SupeedTOTP.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SupeedTOTP.UI.ViewModels"
        mc:Ignorable="d"
        Title="身份验证器 - SupeedTOTP" Height="600" Width="350"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="CopyButton">
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBlock" x:Key="TokenText">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
        
        <!-- ListBox 自定义样式 -->
        <Style TargetType="ListBox" x:Key="CustomListBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.Background" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid Background="#F5F5F5">
        <!-- 标题栏 -->
        <Grid Background="#F5F5F5" Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="⚙" FontSize="16" Background="Transparent" BorderThickness="0" Margin="10,0,0,0" Click="Settings_Click"/>
            <TextBlock Grid.Column="1" Text="身份验证器" FontSize="18" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Button Grid.Column="2" Content="✏" FontSize="16" Background="Transparent" BorderThickness="0" Margin="0,0,10,0" Click="EditAccounts_Click"/>
        </Grid>
        
        <!-- 主内容区 -->
        <Grid Margin="0,50,0,0">
            <!-- 账号列表 -->
            <ListBox x:Name="AccountsList" Margin="10,10,10,10"
                     ItemsSource="{Binding Accounts}"
                     SelectedItem="{Binding SelectedAccount}"
                     Style="{StaticResource CustomListBox}" Background="#F5F5F5" BorderThickness="0">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="vm:AccountViewModel">
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" Background="White" Margin="0,0,0,10" CornerRadius="4">
                            <Grid Margin="15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 服务商名称 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Issuer}" FontSize="14" Foreground="#333333" FontWeight="Medium"/>
                                
                                <!-- 令牌 -->
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding CurrentToken}" FontSize="24" Foreground="#0078D4" FontWeight="Bold" Margin="0,5,0,5"/>
                                
                                <!-- 账号信息 -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Name}" FontSize="12" Foreground="#666666"/>
                                
                                <!-- 复制按钮 -->
                                <Button Grid.Row="1" Grid.Column="1" Content="🌙" FontSize="16" Background="Transparent" BorderThickness="0" 
                                        Command="{Binding CopyTokenCommand}" Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            
            <!-- 空状态提示 -->
            <TextBlock x:Name="EmptyStateText" Grid.Row="1" Text="暂无账号，请点击'添加账号'按钮添加" 
                       FontSize="16" Foreground="#666666" HorizontalAlignment="Center" 
                       VerticalAlignment="Center" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
