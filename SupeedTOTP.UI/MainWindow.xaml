<Window x:Class="SupeedTOTP.UI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:SupeedTOTP.UI.ViewModels"
        xmlns:local="clr-namespace:SupeedTOTP.UI.Views"
        mc:Ignorable="d"
        Title="身份验证器 - SupeedTOTP" Height="400" Width="320"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <!-- ListBox 自定义样式 -->
        <Style TargetType="ListBox" x:Key="CustomListBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <!-- 百分比到宽度的转换器 -->
        <local:PercentageToWidthConverter x:Key="PercentageToWidthConverter"/>
    </Window.Resources>
    <Grid Background="#F5F5F5">
        <!-- 标题栏 -->
        <Grid Background="#F5F5F5" Height="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="⚙" FontSize="16" Background="Transparent" BorderThickness="0" Margin="10,0,0,0" Click="Settings_Click"/>
            <TextBlock Grid.Column="1" Text="身份验证器" FontSize="18" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Button Grid.Column="2" Content="✏" FontSize="16" Background="Transparent" BorderThickness="0" Margin="0,0,10,0" Click="EditAccounts_Click"/>
        </Grid>
        
        <!-- 主内容区 -->
        <Grid Margin="0,50,0,0">
            <!-- 账号列表 -->
            <ListBox x:Name="AccountsList" Margin="10,10,10,10"
                     ItemsSource="{Binding Accounts}"
                     SelectedItem="{Binding SelectedAccount}"
                     Style="{StaticResource CustomListBox}">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="vm:AccountViewModel">
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" Background="White" Margin="10,10,10,10" CornerRadius="4">
                            <Grid Margin="15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- 服务商名称 -->
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Issuer}" FontSize="14" Foreground="#333333" FontWeight="Medium"/>
                                
                                <!-- 令牌 -->
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding CurrentToken}" FontSize="24" Foreground="#0078D4" FontWeight="Bold" Margin="0,5,0,5"/>
                                
                                <!-- 账号信息 -->
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Name}" FontSize="12" Foreground="#666666"/>
                                
                                <!-- 电池样式倒计时 -->
                                <Grid Grid.Row="1" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="5"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="20" Height="12" BorderBrush="#0078D4" BorderThickness="1" CornerRadius="2">
                                        <Rectangle Width="{Binding RemainingSecondsPercentage, Converter={StaticResource PercentageToWidthConverter}, ConverterParameter=18}" Height="10" Fill="#0078D4" Margin="0" HorizontalAlignment="Left"/>
                                    </Border>
                                    <Border Grid.Column="1" Width="3" Height="8" Background="#0078D4" Margin="0,2,0,0" CornerRadius="1"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            
            <!-- 空状态提示 -->
            <TextBlock x:Name="EmptyStateText" Text="暂无账号，请点击'添加账号'按钮添加" 
                       FontSize="16" Foreground="#666666" HorizontalAlignment="Center" 
                       VerticalAlignment="Center" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
